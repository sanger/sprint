package uk.ac.sanger.sprint.model;

/**
 * The protocol used for sending a job status query to a printer.
 * Not all printers must support a status protocol
 */
public enum StatusProtocol {
    SOAP
}
